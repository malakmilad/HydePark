'use strict';

/**
 * news-later-form controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::news-later-form.news-later-form');
